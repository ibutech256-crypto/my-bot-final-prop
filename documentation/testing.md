# Testing Guide

Run `scripts/test.sh` locally or inside the backend container. Add model, API, Celery, WebSocket, MT5 adapter and payment webhook tests for every new feature. Use isolated test databases and never connect tests to live trading accounts.
