# Environment Configuration

Copy `.env.example` to `.env` and set secrets for Django, PostgreSQL, Redis, Telegram, email and MT5. Store production secrets in a vault or protected environment manager. Never commit real broker credentials or payment gateway keys.
