# Maintenance Guide

Monitor logs in `logs/`, Docker health checks, PostgreSQL storage, Redis memory, Celery queues, broker heartbeats and Telegram delivery failures. Rotate secrets, backup PostgreSQL daily, test restores monthly and review audit logs for privileged activity.
